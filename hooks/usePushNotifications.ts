// Push notifications disabled — using real-time Convex queries instead (see Home screen).
// Re-enable this once Firebase/FCM credentials are configured (see docs.expo.dev/push-notifications/fcm-credentials).

export function usePushNotifications() {
  // intentionally no-op
}